\section{Lesson 14}

\subsection{Memory Items}
\begin{enumerate}
    \item The pivot columns are a basis for the column space.
    \item Inclusion relations for subspaces can be identified through Gauss-Jordan.
    \item Bases for an intersection can be identified through Gauss-Jordan.
    \item Bases for a sum can be identified through Gauss-Jordan.
\end{enumerate}

\subsection{Other Remarks}
\begin{enumerate}
    \item \textbf{Inclusion}: If $\text{pref}[\begin{matrix}A & B\end{matrix}]$ has no pivots in the columns of $B$, then $\text{col}\,B \subset \text{col}\,A$.
    \item $\text{col}\,A = \text{col}\,B$ iff $\text{col}\,A \subset \text{col}\,B$ and $\text{col}\,B \subset \text{col}\,A$.
     \item TODO - basis vectors for intersection
     \item $U+V = U\cup V$ for subspaces $U$, $V$.
    \item $\text{dim}(U+V)~=~ \text{dim}(U)~+~\text{dim}(V)~-~\text{dim}(U\cap V)$
\end{enumerate}